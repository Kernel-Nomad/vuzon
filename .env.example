# Token scopes: Account → Email Routing Addresses (Read & Edit), Zone → Email Routing Rules (Read & Edit).
# https://developers.cloudflare.com/fundamentals/api/get-started/create-token/

CF_API_TOKEN=
DOMAIN=
AUTH_USER=admin
AUTH_PASS=change_this_password

# Recommended with Docker: stable sessions across restarts (openssl rand -hex 32)
# SESSION_SECRET=

# Host port (optional; default 8001 in docker-compose.yml)
# VUZON_PORT=8001

# Reverse proxy: TRUST_PROXY=1
# If autodetection fails: CF_ZONE_ID= and CF_ACCOUNT_ID=
